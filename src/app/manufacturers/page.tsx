import { FactoryDirectory } from "@/components/FactoryDirectory";
export const metadata = { title: "Manufacturing Network | MISO JAPAN", description: "Explore sample capability profiles in the MISO JAPAN manufacturing network." };
export default function ManufacturersPage() { return <FactoryDirectory locale="en" />; }
